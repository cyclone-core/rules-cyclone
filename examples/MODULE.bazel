"""examples：演示如何把 rules_cyclone 接入一个真实工程。

默认走 rules_cyclone 自带的预编译 CLI 工具链（首次解析按宿主平台
从 Release 惰性下载），一条 bazel_dep 即接入。
本地引擎开发改用 shim 工具链（python -m cyclone 驱动本机源码）：
    bazel test --extra_toolchains=//:local_cyclone_toolchain //...
（根模块的 extra_toolchains 优先于 rules_cyclone 注册的默认工具链；
 也可把这行写进本机 user.bazelrc，见 .bazelrc 的 try-import。）
"""

module(name = "rules_cyclone_examples", version = "0.0.0")

bazel_dep(name = "rules_cyclone", version = "0.1.0")
local_path_override(module_name = "rules_cyclone", path = "..")
